"""dashboard_v2/execution

Frozen-snapshot copies of the root ``execution/`` scripts as of 2026-04-20.
These copies will be refactored to expose pure functions for the new
Streamlit app (``dashboard_v2/app.py``). The root ``execution/`` folder stays
untouched so chef can keep running active photo/data work on the originals.

At cut-over we decide whether to merge these back or keep separate.
"""
